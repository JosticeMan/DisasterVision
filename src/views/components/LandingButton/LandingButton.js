import React, {Component} from 'react';

class LandingButton extends Component {
    render() {
        return (
            <button>

            </button>
        );
    }
}

export default LandingButton;